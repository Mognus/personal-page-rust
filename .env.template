# --- Postgres ---
POSTGRES_DB=personal_page
POSTGRES_USER=personal_page
POSTGRES_PASSWORD=change_me
POSTGRES_PORT=5432

# --- Backend server ---
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
DATABASE_URL=postgres://personal_page:change_me@localhost:5432/personal_page
JWT_SECRET=change_me
JWT_EXPIRES_IN_SECONDS=3600

# --- Frontend (Next BFF) ---
# Backend base URL as seen from inside the compose network (service name, not
# localhost). Used by the Next callApi wrapper.
BACKEND_URL=http://backend:8080
