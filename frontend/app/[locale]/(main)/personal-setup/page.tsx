// No active config: the layout's grid shows the floating buttons. Content
// appears only on /personal-setup/[slug], morphed into the expanded cell.
export default function PersonalSetup() {
    return null;
}
